/*
 * Author : Sowndariya
 * PaymentStatus is an enumeration that represents the
 * outcome of a payment transaction: SUCCESS, FAILED,
 * or PENDING, used to track payment state.
 */
 
package com.ems.enums;

public enum PaymentStatus {
	SUCCESS,
	FAILED,
	PENDING, 
	REFUNDED
}
